baseurl = "https://frklan.github.io/"
title = "Fredrik Andersson"
DefaultContentLanguage = "sv"
theme = "hyde-x"

#[params]
#  GithubID = "frklan"
#  LinkedInID = "your_linkedin"
#  FacebookID = "your_facebook"
#  Name = "Fredrik Andersson"
#  HeaderUsername = "Fredrik"
#  HeaderHostname = "frklan.github.io"
#  Email = "fredrik.k.andersson@gmail.com"
#  About = "a random guy doing stuff"
#  ProfilePicture = "https://farm8.staticflickr.com/7353/buddyicons/39190652@N03_r.jpg?1378581704"
# *******
  [author]
      name = "Fredrik Andersson"

  [permalinks]

      post = "/blog/:year/:month/:day/:title/"

  [taxonomies]
      # Optional. Use if you want tags and lists.
      category = "categories"

  [params]
      # If false display full article contents in blog index.
      # Otherwise show description and 'read on' link to individual blog post page.
      # Default (if omitted) is true.
      truncate = true

      # Used when a given page doesn't set its own.
      defaultDescription = "..."
      defaultKeywords = "arduino,openhardware,gnu"

      # Hide estimated reading time for posts.
      # Default (if omitted) is false.
      hideReadingTime = true

      # Changes sidebar background and link/accent colours.
      # See below for more colour options.
      # This also works: "theme-base-08 layout-reverse", or just "layout-reverse".
      # theme = "layout"

      # Select a syntax highight.
      # Check the static/css/highlight directory for options.
      highlight = "sunburst"

      # Optional additional custom CSS file URL, will override other styles.
      customCSS = ""

      # Displays under the author name in the sidebar, keep it short.
      # You can use markdown here.
      tagline = "{they shot him down}<br />{they thought he was a monster}<br />{but he was the king}"

      # Text for the top menu link, which goes the root URL for the site.
      # Default (if omitted) is "Blog".
      home = "blog"

      # Metadata used to drive integrations.
      # googleAnalytics = "Your Google Analytics tracking code"
      # gravatarHash = "MD5 hash of your Gravatar email address"

      # Sidebar social links, these must be full URLs.
      github = "https://github.com/frklan"
      # gitlab = "https://gitlab.com/frklan"
      # bitbucket = ""
      # stackOverflow = ""
      linkedin = "https://se.linkedin.com/in/fredrik-andersson-71585830"
      # googleplus = ""
      facebook = "https://www.facebook.com/fredrik.andersson.330467"
      # twitter = ""
      # youtube = ""
      flickr = "https://secure.flickr.com/photos/39190652@N03/"
      instagram = "https://www.instagram.com/frklan/"

      # Other social-like sidebar links
      rss = false  # switch to true to enable RSS icon link
      # flattr = ""  # populate with your flattr uid

      SectionPagesMenu = "main"
      # menu = ["blog", "about"]

      gravatarSiteHash = "f97a33f7d66aa4084c4d9c94b49fd74d"
      #gravatarHash = "f97a33f7d66aa4084c4d9c94b49fd74d"

  [menu]
    [[menu.main]]
      name = "om mig"
      weight = 1
      url = "/about"
